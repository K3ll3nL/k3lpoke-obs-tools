{
  "name": "k3lpoke-marketplace",
  "version": "0.1.0",
  "description": "K3lPoke marketplace — core infrastructure and apps",
  "author": "K3lPoke",
  "private": true,
  "packageManager": "npm@11.0.0",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "turbo run dev --filter=\"./apps/*\"",
    "build": "turbo run build --filter=\"./apps/*\""
  },
  "devDependencies": {
    "turbo": "latest"
  }
}
